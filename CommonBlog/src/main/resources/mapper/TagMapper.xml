<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.blog.mapper.TagMapper">
    <select id="selectTagByArticleId" resultType="java.lang.Long">
        select t.id
        from sg_article a LEFT JOIN sg_article_tag sat on a.id=sat.article_id
                          LEFT JOIN sg_tag t on sat.tag_id=t.id
        where a.id=#{ArticleId}
    </select>
</mapper>